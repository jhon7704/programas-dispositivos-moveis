fun main() {
  val statusluztransito = "verde" 
    
    val acaotransito = when (statusluztransito) {
        "verde" -> "siga"
        "amarelo" -> "atenção"
        "vermelho" -> "pare"
        else -> "não esta funcionando"
    }
    println(acaotransito)
}
